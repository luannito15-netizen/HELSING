# unity-bootstrap — LEGACY / DO NOT USE

Este projeto Unity **não é mais o projeto ativo** do HELSING.

O projeto oficial de produção é:

`../unity/`

Esta pasta é mantida apenas como referência histórica. Não iniciar gameplay, não adicionar assets de produção e não usar como base para novas cenas aqui.

Este arquivo foi adicionado na raiz do projeto (fora de `Assets/`, `Library/` e `ProjectSettings/`) e não altera nenhum estado do Editor, cena, prefab ou configuração existente.
